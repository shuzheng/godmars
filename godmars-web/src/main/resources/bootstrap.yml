spring:
  application:
    name: godmars-web
  cloud:
    config:
      discovery:
        enabled: true
        service-id: godmars-config
      label: master
      profile: dev
      username: admin
      password: 123456
  zipkin:
    base-url: http://localhost:60001

server:
  # 端口
  port: 30001

eureka:
  client:
    serviceUrl:
      defaultZone: http://peer1:10001/eureka/